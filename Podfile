# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'BootcampFirebase' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for BootcampFirebase
pod 'Firebase/Core'
pod 'Firebase/Auth'

  target 'BootcampFirebaseTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'BootcampFirebaseUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
